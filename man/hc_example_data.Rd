% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_example_data.R
\name{hc_example_data}
\alias{hc_example_data}
\title{Load an example \code{HCoCenaExperiment}}
\usage{
hc_example_data(
  stage = c("prepared", "after_part1", "after_part2", "clustered"),
  dir_output = NULL
)
}
\arguments{
\item{stage}{Workflow stage to load:
\itemize{
\item \code{"prepared"}: data read and all settings made;
\item \code{"after_part1"}: correlations computed and the cutoff chosen
(\code{\link[=hc_run_expression_analysis_1]{hc_run_expression_analysis_1()}}, \code{\link[=hc_set_cutoff]{hc_set_cutoff()}});
\item \code{"after_part2"}: group fold changes computed
(\code{\link[=hc_run_expression_analysis_2]{hc_run_expression_analysis_2()}});
\item \code{"clustered"}: integrated network built and modules detected
(\code{\link[=hc_build_integrated_network]{hc_build_integrated_network()}}, \code{\link[=hc_cluster_calculation]{hc_cluster_calculation()}}).
}}

\item{dir_output}{Output directory to store in the object. Defaults to a
new directory inside \code{\link[=tempdir]{tempdir()}}.}
}
\value{
A \code{HCoCenaExperiment}.
}
\description{
Returns one of the small example objects shipped in \code{inst/extdata}, taken
at a given stage of the standard workflow, so that individual steps can be
tried without running everything before them. The data are two simulated
layers of 40 marker genes for four blood cell types over 32 samples; see
\code{inst/scripts/make-fixtures.R} for the generating code.
}
\details{
The output directory stored in the object is replaced by a fresh
directory inside \code{\link[=tempdir]{tempdir()}}, so functions that write plots or tables
never write outside the session's temporary directory.
}
\examples{
hc <- hc_example_data("clustered")
hc
table(hc_gene_to_cluster(hc)$color)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_longitudinal_endotypes.R
\name{hc_longitudinal_module_means}
\alias{hc_longitudinal_module_means}
\title{Build longitudinal module means per donor and timepoint}
\usage{
hc_longitudinal_module_means(
  hc,
  donor_col = "donor",
  time_col = "time_point",
  layer = NULL,
  group_col = NULL,
  modules = NULL,
  use_module_labels = TRUE,
  time_levels = NULL,
  aggregate_fun = function(x) base::mean(x, na.rm = TRUE),
  value_label = NULL,
  impute_missing = c("linear", "locf", "none"),
  min_genes_per_module = 5,
  slot_name = "longitudinal_module_means"
)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment}.}

\item{donor_col}{Annotation column holding donor/patient IDs.}

\item{time_col}{Annotation column holding ordered timepoint labels.}

\item{layer}{Layer to use (index, layer id, or layer name). Defaults to the
first available layer.}

\item{group_col}{Optional annotation column (e.g. outcome group) to carry.}

\item{modules}{Optional subset of modules (module labels or color names).}

\item{use_module_labels}{Logical. If \code{TRUE}, use \code{M}-style labels from
\code{hc_plot_cluster_heatmap()} when available; otherwise use color names.}

\item{time_levels}{Optional explicit timepoint order.}

\item{aggregate_fun}{Function used to aggregate technical replicates per
donor/timepoint/module (default \code{mean}).}

\item{value_label}{Optional y-axis label base (e.g. \code{"Module mean GFC"}).
If \code{NULL}, an automatic label is inferred from the selected layer id.}

\item{impute_missing}{One of \code{"none"}, \code{"linear"}, \code{"locf"} to impute missing
donor-timepoint values within each donor/module trajectory.}

\item{min_genes_per_module}{Minimum number of overlapping genes required to
keep a module.}

\item{slot_name}{Name of the satellite slot to store outputs.}
}
\value{
Updated \code{HCoCenaExperiment} with longitudinal data in
\code{hc@satellite[[slot_name]]}.
}
\description{
Computes module-level mean expression trajectories from the clustered modules
in an \code{HCoCenaExperiment}. This creates a donor-by-(module x timepoint)
feature matrix that can be used for longitudinal endotype discovery.
}
\examples{
hc <- hc_example_data("clustered")
hc <- hc_longitudinal_module_means(hc, donor_col = "donor", time_col = "timepoint")
}

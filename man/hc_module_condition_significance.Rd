% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_api.R
\name{hc_module_condition_significance}
\alias{hc_module_condition_significance}
\title{Test module differences between conditions (S4 API)}
\usage{
hc_module_condition_significance(
  hc,
  set = "all",
  condition_col = NULL,
  donor_col = NULL,
  time_col = NULL,
  run_wilcox = TRUE,
  run_limma = TRUE,
  run_lmm = FALSE,
  lmm_include_time = TRUE,
  limma_reference = NULL,
  limma_trend = TRUE,
  standardize_modules = FALSE,
  padj = "BH",
  export_excel = TRUE,
  excel_file = "Module_condition_significance.xlsx",
  slot_name = "module_condition_significance"
)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment}.}

\item{set}{Integer vector of layer indices or \code{"all"} (default).}

\item{condition_col}{Column name in annotation files defining conditions.
If \code{NULL}, uses \code{hcobject[["global_settings"]][["voi"]]}.}

\item{donor_col}{Optional donor identifier column in annotation files.
Required if \code{run_lmm = TRUE}.}

\item{time_col}{Optional time column in annotation files (used by LMM when
available and \code{lmm_include_time = TRUE}).}

\item{run_wilcox}{Logical; run Wilcoxon/Kruskal module-wise test.}

\item{run_limma}{Logical; run limma pairwise contrasts.}

\item{run_lmm}{Logical; run linear mixed model (\code{nlme::lme}) per module.}

\item{lmm_include_time}{Logical; include \code{time_col} as fixed effect in LMM
when available.}

\item{limma_reference}{Optional reference condition for one-vs-reference
limma contrasts. If \code{NULL}, all pairwise contrasts are tested.}

\item{limma_trend}{Logical; passed to \code{limma::eBayes(trend = ...)}.}

\item{standardize_modules}{Logical; z-score each module across the samples
of a layer before testing. Raw module means carry the module's absolute
expression level, so limma's variance moderation - which borrows
information across modules - is dominated by the highly expressed ones,
and the reported effect sizes are not comparable between modules.

The standardization is applied per layer, so with several layers pooled
(\code{set = "all"}) it also removes the between-layer offset. With a single
layer the rank-based Wilcoxon/Kruskal p-values are unchanged, because
z-scoring is monotone within a row; pooled across layers they do change,
because the layers are brought onto a common scale first. That is usually
what you want when the layers are different tissues or assays, since
neither the limma design (\code{~ 0 + condition}) nor the LMM
(\code{value ~ condition}, random \code{~ 1 | donor}) carries a layer term.

Default \code{FALSE} keeps the previous behaviour.}

\item{padj}{Multiple-testing correction method (passed to \code{p.adjust}/limma).}

\item{export_excel}{Logical; write result tables to Excel in save folder.}

\item{excel_file}{File name for the Excel export.}

\item{slot_name}{Satellite slot name for storing results.}
}
\value{
Updated \code{HCoCenaExperiment}.
}
\description{
Test module differences between conditions (S4 API)
}
\examples{
hc <- hc_example_data("clustered")
hc <- hc_module_condition_significance(
  hc,
  condition_col = "group",
  run_limma = FALSE,
  export_excel = FALSE
)
}

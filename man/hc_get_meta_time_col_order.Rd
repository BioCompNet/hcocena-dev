% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_longitudinal_quick.R
\name{hc_get_meta_time_col_order}
\alias{hc_get_meta_time_col_order}
\title{Get column order for meta-cluster x timepoint heatmaps}
\usage{
hc_get_meta_time_col_order(
  hc,
  slot_name = "longitudinal_endotypes",
  layer = NULL
)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment}.}

\item{slot_name}{Satellite slot containing \code{meta_time_grouping}, or a base
prefix such as \code{"longitudinal_endotypes"} that resolves matching suffixed
slots.}

\item{layer}{Layer index, layer id/name, or \code{NULL} for combined order across
all resolved layers.}
}
\value{
Character vector with heatmap column order as expected by
\code{hc_change_grouping_parameter()}.
}
\description{
Returns the column-order vector stored by \code{hc_add_meta_time_grouping()}.
}
\examples{
hc <- hc_example_data("clustered")
hc <- hc_longitudinal_step1_module_donor(
  hc,
  donor_col = "donor",
  time_col = "timepoint",
  time_levels = c("T1", "T2"),
  k = 2,
  nstart = 1,
  cap_runs = 1,
  impute = FALSE,
  seed = 1
)$hc
hc <- hc_longitudinal_endotype_clustering(hc, k = 2, nstart = 1, seed = 1)
hc <- hc_longitudinal_meta_clustering(
  hc,
  feature_source = "feature_matrix_used",
  k = 2:3,
  method = "kmeans",
  consensus = TRUE,
  seed = 1
)
hc <- hc_add_meta_time_grouping(hc, donor_col = "donor", time_col = "timepoint")
hc_get_meta_time_col_order(hc)
}

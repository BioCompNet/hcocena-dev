% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_regroup_samples.R
\name{hc_cut_hclust}
\alias{hc_cut_hclust}
\title{Cut a hierarchical clustering tree to regroup samples (S4 API)}
\usage{
hc_cut_hclust(
  hc,
  by = "all",
  set = "all",
  method = "complete",
  k,
  save = FALSE
)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment}.}

\item{by}{What to regroup on: \code{"all"} (default, all genes; dendrogram only,
for memory reasons), \code{"network"} (network genes) or \code{"module"} (mean
expression per module). The latter two also draw a heatmap.}

\item{set}{Layers to regroup: \code{"all"} (default) or an integer vector of
layer indices.}

\item{method}{Agglomeration method passed to \code{\link[stats:hclust]{stats::hclust()}}.
Default is \code{"complete"}.}

\item{k}{Integer vector giving, per regrouped layer, the number of clusters
to cut the dendrogram into. Defaults to 1 per layer (no cutting - good for
a first look).}

\item{save}{Logical. If \code{TRUE}, overwrite the sample groups and export a new
annotation file with the \verb{_regrouped} suffix; the original groups are kept
in a \verb{<voi>_old} column. Default is \code{FALSE}.}
}
\value{
Updated \code{HCoCenaExperiment}.
}
\description{
Assigns new sample group labels from the data structure rather than from
metadata, by cutting a hierarchical clustering tree. Useful when the variable
of interest does not match the structure seen in the heatmaps or the PCA.
}
\details{
Re-run with \code{save = FALSE} to try different \code{k} values; only set
\code{save = TRUE} once you have settled on one, since that overwrites the
original groups and exports a new annotation file (without row names). After
saving, re-run the pipeline from \code{\link[=hc_run_expression_analysis_2]{hc_run_expression_analysis_2()}} onward.
}
\examples{
hc <- hc_example_data("clustered")
hc <- hc_cut_hclust(hc, by = "module", k = c(2, 2), save = FALSE)
}

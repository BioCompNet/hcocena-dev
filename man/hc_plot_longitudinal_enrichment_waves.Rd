% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_longitudinal_endotypes.R
\name{hc_plot_longitudinal_enrichment_waves}
\alias{hc_plot_longitudinal_enrichment_waves}
\title{Plot longitudinal enrichment waves for top terms per module}
\usage{
hc_plot_longitudinal_enrichment_waves(
  hc,
  slot_name = "longitudinal_module_means",
  databases = "Kegg",
  top = 5,
  custom_terms = NULL,
  term_match = c("exact", "contains", "regex"),
  enrichment_table = c("selected", "all", "significant"),
  score_method = c("ssgsea", "zscore", "rank_mean"),
  score_scale = c("none", "z"),
  layer = NULL,
  donor_col = NULL,
  time_col = NULL,
  time_levels = NULL,
  min_term_genes = 5,
  impute_missing = c("auto", "linear", "locf", "none"),
  qvalue_max = NULL,
  show_donor_lines = TRUE,
  facet_ncol = 4,
  free_y = TRUE,
  save_pdf = TRUE,
  file_prefix = "Longitudinal_Enrichment_Waves",
  save_width = 13,
  save_height = 9,
  export_excel = TRUE
)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment}.}

\item{slot_name}{Satellite slot with longitudinal settings/module lookup.
Usually \code{"longitudinal_module_means"}.}

\item{databases}{Character vector of databases to plot.}

\item{top}{Number of top terms per module.}

\item{custom_terms}{Optional custom term filter. Can be a character vector
(applied to all modules) or a named list with module names (e.g. \code{M1}) as
keys and character vectors of terms as values. If set, matched terms are
plotted instead of automatic top-term selection.}

\item{term_match}{Matching mode for \code{custom_terms}: \code{"exact"},
\code{"contains"}, or \code{"regex"}.}

\item{enrichment_table}{Which enrichment table to use:
\code{"selected"} (default), \code{"all"}, or \code{"significant"}.}

\item{score_method}{One of \code{"ssgsea"} (default), \code{"zscore"}, or \code{"rank_mean"}.}

\item{score_scale}{Plot-only scaling: \code{"none"} (default) or \code{"z"} to
z-standardize scores within each module-term before plotting.}

\item{layer}{Optional layer selector (index/id/name). If \code{NULL}, uses
\code{layer_id} from the longitudinal slot.}

\item{donor_col, time_col}{Optional donor/time annotation columns. If \code{NULL},
values from the longitudinal slot are used.}

\item{time_levels}{Optional explicit time ordering.}

\item{min_term_genes}{Minimum number of matched genes per term.}

\item{impute_missing}{One of \code{"auto"}, \code{"linear"}, \code{"locf"}, \code{"none"}.}

\item{qvalue_max}{Optional q-value filter before top-term selection.}

\item{show_donor_lines}{Logical; add faint donor lines behind term means.}

\item{facet_ncol}{Number of facet columns.}

\item{free_y}{Logical; free y-axis per module facet.}

\item{save_pdf}{Logical; save one PDF per database.}

\item{file_prefix}{Prefix used for output files.}

\item{save_width, save_height}{PDF size in inches.}

\item{export_excel}{Logical; export top-term + trajectory tables to Excel.}
}
\value{
A list with \code{plots}, \code{top_terms}, \code{mean_trajectories},
\code{donor_trajectories}, and \code{score_method_used}.
}
\description{
Uses enrichment outputs from \code{\link[=hc_functional_enrichment]{hc_functional_enrichment()}} to select top terms
per module for one or multiple databases (for example \code{Kegg}), then computes
sample-level term activity scores and visualizes trajectories across
donor/timepoint in module-faceted wave plots.
}
\details{
Scoring uses GSVA ssGSEA when available (\code{score_method = "ssgsea"}), with a
rank-based fallback if GSVA is unavailable.
}
\examples{
hc <- hc_example_data("clustered")
gmt <- system.file("extdata", "toy_celltype_markers.gmt", package = "hcocena")
hc <- hc_functional_enrichment(
  hc,
  gene_sets = character(0),
  custom_gmt_files = c(CellTypes = gmt),
  universe = "network"
)
hc <- hc_longitudinal_module_means(hc, donor_col = "donor",
                                   time_col = "timepoint")
# Database names are title-cased by hc_functional_enrichment().
waves <- hc_plot_longitudinal_enrichment_waves(
  hc,
  databases = "Celltypes",
  top = 1,
  min_term_genes = 3,
  score_method = "rank_mean",
  save_pdf = FALSE,
  export_excel = FALSE
)
waves$top_terms
}

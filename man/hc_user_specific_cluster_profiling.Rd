% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_user_specific_cluster_profiling.R
\name{hc_user_specific_cluster_profiling}
\alias{hc_user_specific_cluster_profiling}
\title{Profile modules against user-defined enrichment terms}
\usage{
hc_user_specific_cluster_profiling(
  hc,
  from_file = FALSE,
  path = NULL,
  enrichment_keys = NULL,
  db = c("Go", "Kegg", "Hallmark"),
  padj = "BH",
  qval = 0.1
)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment}.}

\item{from_file}{Logical. If \code{TRUE}, read the terms of interest from \code{path}
instead of \code{enrichment_keys}. Default is \code{FALSE}.}

\item{path}{Path to the file holding the terms of interest. Only used when
\code{from_file = TRUE}.}

\item{enrichment_keys}{Character vector of terms to profile the modules
against. Required when \code{from_file = FALSE}.}

\item{db}{Databases to search for the terms. Default is
\code{c("Go", "Kegg", "Hallmark")}.}

\item{padj}{Multiple-testing correction method. Default is \code{"BH"}.}

\item{qval}{Maximum adjusted p-value for a term to count. Default is 0.1.}
}
\value{
Updated \code{HCoCenaExperiment}.
}
\description{
Scores every module against a user-supplied list of terms of interest, either
given directly via \code{enrichment_keys} or read from a file.
}
\examples{
hc <- hc_example_data("clustered")
gmt <- tempfile(fileext = ".gmt")
writeLines(
  c(
    paste(c("T cells", "-", "CD3D", "CD3E", "CD3G", "CD2", "CD28",
            "LCK", "ZAP70", "IL7R", "CD7", "TRAC"), collapse = "\t"),
    paste(c("B cells", "-", "CD19", "MS4A1", "CD79A", "CD79B", "BLNK",
            "PAX5", "CR2", "FCRL1", "TNFRSF13B", "VPREB3"), collapse = "\t"),
    paste(c("Monocytes", "-", "CD14", "LYZ", "FCN1", "VCAN", "S100A8",
            "S100A9", "CSF1R", "ITGAM", "CD68", "FCGR3A"), collapse = "\t"),
    paste(c("NK cells", "-", "NKG7", "GNLY", "KLRD1", "KLRF1", "PRF1",
            "GZMB", "NCR1", "KLRC1", "FGFBP2", "SPON2"), collapse = "\t")
  ),
  gmt
)
hc <- hc_functional_enrichment(
  hc,
  gene_sets = character(0),
  custom_gmt_files = c(CellTypes = gmt)
)
hc <- hc_user_specific_cluster_profiling(hc, from_file = FALSE)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_api.R
\name{hc_upstream_inference}
\alias{hc_upstream_inference}
\title{Upstream regulator/pathway inference (S4 API)}
\usage{
hc_upstream_inference(
  hc,
  resources = c("TF", "Pathway"),
  top = 5,
  clusters = c("all"),
  padj = "BH",
  qval = 0.05,
  tf_confidence = c("A", "B", "C"),
  minsize = 5,
  method = "ulm",
  activity_input = "gfc",
  fc_comparisons = NULL,
  custom_pathway_gmt = NULL,
  heatmap_side = "left",
  cluster_columns = FALSE,
  heatmap_cluster_columns = NULL,
  col_order = NULL,
  heatmap_col_order = NULL,
  gfc_scale_limits = NULL,
  plot = TRUE,
  save_pdf = TRUE,
  pdf_width = NULL,
  pdf_height = NULL,
  pdf_pointsize = 11,
  plot_per_comparison = TRUE,
  consistent_terms = TRUE,
  overall_plot_scale = 1
)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment}.}

\item{resources}{Character vector of upstream resources to use.
Allowed values are \code{"TF"} and \code{"Pathway"}. Default is both.}

\item{top}{Integer. Number of top significant regulators/pathways to keep
per module and resource in the selected summary. Default is 5.}

\item{clusters}{Either \code{"all"} (default) or a character vector of module
colors to process.}

\item{padj}{Multiple-testing correction method passed to
\code{\link[stats:p.adjust]{stats::p.adjust()}}. Default is \code{"BH"}.}

\item{qval}{Adjusted p-value threshold for significance. Default is 0.05.}

\item{tf_confidence}{Character vector of DoRothEA confidence levels to keep.
Default is \code{c("A", "B", "C")}.}

\item{minsize}{Minimum number of targets required per source in
\code{decoupleR::run_ulm()}. Default is 5.}

\item{method}{Inference method name used via \verb{decoupleR::run_<method>}.
Currently only \code{"ulm"} is supported. Default is \code{"ulm"}.}

\item{activity_input}{Character scalar selecting the matrix used for
decoupleR activity inference:
\code{"gfc"} (default) uses \code{integrated_output$GFC_all_layers},
\code{"fc"} uses user-defined pairwise fold-changes from \code{fc_comparisons},
\code{"expression"} uses layer-wise mean expression values (anti-log transformed
when \code{data_in_log = TRUE}) across samples.}

\item{fc_comparisons}{Character vector of pairwise comparisons used only when
\code{activity_input = "fc"}. Each entry must be formatted as
\code{"groupA_vs_groupB"} (numerator vs denominator), e.g.
\code{c("IFNg_seq_vs_baseline_seq", "IL4_seq_vs_baseline_seq")}.}

\item{custom_pathway_gmt}{Optional custom pathway GMT file(s) added to the
pathway inference resource. Accepts a character vector or named list of
file paths. Paths can be absolute/relative or file names inside
\code{dir_reference_files}.}

\item{heatmap_side}{Position of the hCoCena heatmap in the combined output.
Choose one of \code{"left"} (default) or \code{"right"}.}

\item{cluster_columns}{Logical. If \code{FALSE} (default), reuse the
column order from the main hCoCena heatmap when available. If \code{TRUE},
cluster the columns for this upstream plot instead.}

\item{heatmap_cluster_columns}{Legacy alias for \code{cluster_columns}.}

\item{col_order}{Optional character vector overriding the hCoCena
heatmap column order for this upstream plot only. If \code{NULL} (default),
the column order from the main module heatmap is reused when available.}

\item{heatmap_col_order}{Legacy alias for \code{col_order}.}

\item{gfc_scale_limits}{Optional numeric vector controlling the module-heatmap
color scale limits used in upstream combined heatmaps (left/right hCoCena
panel). Provide one positive number (\code{x} -> \code{c(-x, x)}) or two numbers
(\code{c(min, max)}). If NULL, uses stored limits from the main heatmap when
available, otherwise falls back to \code{c(-range_GFC, range_GFC)}.}

\item{plot}{Logical; if \code{TRUE} (default), draw plot outputs in the active
graphics device.}

\item{save_pdf}{Logical; if \code{TRUE} (default), export plots to
\code{Upstream_Inference.pdf}.}

\item{pdf_width}{Optional numeric width (inches) for \code{Upstream_Inference.pdf}.
If NULL (default), width is auto-estimated from content.}

\item{pdf_height}{Optional numeric height (inches) for \code{Upstream_Inference.pdf}.
If NULL (default), height is auto-estimated from content.}

\item{pdf_pointsize}{Numeric base pointsize used for the upstream PDF device.
Default is 11.}

\item{plot_per_comparison}{Logical; if \code{TRUE}, additionally create one
combined upstream heatmap page per activity column (GFC condition or FC
comparison), each with the matching one-column module heatmap.}

\item{consistent_terms}{Logical; controls term comparability when
\code{plot_per_comparison = TRUE}.
If \code{TRUE}, per-comparison pages still show only values from the currently
shown condition, but use a global (all-condition) term axis for
comparability; \code{*} marks significance for the currently shown condition.
If \code{FALSE}, each page uses only local selected activities from
the shown condition and no significance marker is drawn.}

\item{overall_plot_scale}{Numeric scaling factor for plot typography and
marker sizes. Default is 1.}
}
\value{
Updated \code{HCoCenaExperiment}.
}
\description{
Upstream regulator/pathway inference (S4 API)
}
\examples{
if (requireNamespace("decoupleR", quietly = TRUE)) {
  hc <- hc_example_data("clustered")
  gmt <- system.file("extdata", "toy_celltype_markers.gmt",
                     package = "hcocena")
  # Pathway activity from a local GMT; the DoRothEA TF priors
  # (`resources = "TF"`) need the `dorothea` package.
  hc <- hc_upstream_inference(
    hc,
    resources = "Pathway",
    custom_pathway_gmt = c(CellTypes = gmt),
    plot = FALSE,
    save_pdf = FALSE
  )
  head(hc_satellite(hc, "upstream_inference")$selected_upstream_all)
}
}

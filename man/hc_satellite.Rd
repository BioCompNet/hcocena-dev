% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R
\name{hc_satellite}
\alias{hc_satellite}
\alias{hc_satellite,HCoCenaExperiment-method}
\title{Access downstream results stored in an \code{HCoCenaExperiment}}
\usage{
hc_satellite(x, name = NULL)

\S4method{hc_satellite}{HCoCenaExperiment}(x, name = NULL)
}
\arguments{
\item{x}{An \code{HCoCenaExperiment}.}

\item{name}{Optional name of a single entry. If \code{NULL} (default), the whole
list is returned.}
}
\value{
A \code{S4Vectors::SimpleList} of stored results, or the entry \code{name}
(\code{NULL} if that entry does not exist).
}
\description{
Downstream steps (module gene lists, enrichments, hub genes, module
statistics, ...) store their results in a named list inside the object.
\code{hc_satellite()} returns that list, or a single entry of it.
}
\examples{
hc <- hc_example_data("clustered")
names(hc_satellite(hc))
}

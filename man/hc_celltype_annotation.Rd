% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_api.R
\name{hc_celltype_annotation}
\alias{hc_celltype_annotation}
\title{Module cell-type annotation from Enrichr (S4 API)}
\usage{
hc_celltype_annotation(
  hc,
  databases = c("Descartes_Cell_Types_and_Tissue_2021", "Human_Gene_Atlas"),
  custom_gmt_files = NULL,
  clusters = c("all"),
  mode = c("coarse", "fine"),
  top = 3,
  qval = 0.1,
  padj = "BH",
  min_term_genes = 5,
  min_gs_size = 10,
  max_gs_size = 5000,
  annotation_slot = c("auto", "enriched_per_cluster", "enriched_per_cluster2"),
  slot_suffix = NULL,
  clear_previous_slots = TRUE,
  coarse_map = NULL,
  coarse_include_other = TRUE,
  refresh_db = FALSE,
  export_excel = TRUE,
  excel_file = "Module_Celltype_Annotation.xlsx",
  plot_heatmap = FALSE,
  heatmap_file_name = "Heatmap_modules_celltype_annotation.pdf",
  ...
)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment}.}

\item{databases}{Character vector of Enrichr library names.}

\item{custom_gmt_files}{Optional custom GMT file(s) to include in the
cell-type annotation. Accepts a character vector or named list of file
paths. Paths can be absolute/relative or file names inside
\code{dir_reference_files}.}

\item{clusters}{Either \code{"all"} (default) or a character vector of module
IDs/colors.}

\item{mode}{Either \code{"coarse"} (broad classes) or \code{"fine"} (specific terms).}

\item{top}{Number of selected categories per module.}

\item{qval}{Maximum adjusted p-value (\code{qvalue}) for significant terms.}

\item{padj}{Multiple-testing correction method for \code{\link[clusterProfiler:enricher]{clusterProfiler::enricher()}}.}

\item{min_term_genes}{Minimum number of genes required per Enrichr term.}

\item{min_gs_size}{Minimum gene-set size used in \code{\link[clusterProfiler:enricher]{clusterProfiler::enricher()}}
(\code{minGSSize}).}

\item{max_gs_size}{Maximum gene-set size used in \code{\link[clusterProfiler:enricher]{clusterProfiler::enricher()}}
(\code{maxGSSize}).}

\item{annotation_slot}{Backward-compatibility option when only one database
is used. With multiple databases, one slot per DB is always written
(\verb{enriched_per_cluster_<db>}), and previous DB slots are reset on each run.}

\item{slot_suffix}{Optional character suffix appended to generated annotation
slots (for example \code{"decoupler"} -> \verb{enriched_per_cluster_<db>_decoupler}).
Useful to keep multiple annotation runs side by side.}

\item{clear_previous_slots}{Logical. If \code{TRUE} (default), previous
\verb{enriched_per_cluster*} slots are removed before writing new results.
Set to \code{FALSE} to keep existing slots.}

\item{coarse_map}{Optional named character vector with regex rules for
\code{mode = "coarse"}. Names are output class labels.}

\item{coarse_include_other}{Logical. Keep unmatched terms as \code{"Other"} in
\code{mode = "coarse"}.}

\item{refresh_db}{Logical. If \code{TRUE}, re-download Enrichr metadata/libraries.}

\item{export_excel}{Logical. If \code{TRUE}, write summary tables to Excel.}

\item{excel_file}{Excel file name in the configured output folder.}

\item{plot_heatmap}{Logical. If \code{TRUE}, run \code{\link[=hc_plot_cluster_heatmap]{hc_plot_cluster_heatmap()}} after
updating annotation slots.}

\item{heatmap_file_name}{File name used when \code{plot_heatmap = TRUE}.}

\item{...}{Further arguments passed through to the underlying
implementation. Every supported argument is documented on this page;
\code{...} only carries the tail of the argument list.}
}
\value{
Updated \code{HCoCenaExperiment}.
}
\description{
Module cell-type annotation from Enrichr (S4 API)
}
\examples{
hc <- hc_example_data("clustered")
gmt <- system.file("extdata", "toy_celltype_markers.gmt", package = "hcocena")
# A local marker GMT instead of the default Enrichr libraries, so that the
# example runs offline:
hc <- hc_celltype_annotation(
  hc,
  databases = character(0),
  custom_gmt_files = c(CellTypes = gmt),
  export_excel = FALSE
)
hc_satellite(hc, "celltype_annotation")$selected_celltypes
}

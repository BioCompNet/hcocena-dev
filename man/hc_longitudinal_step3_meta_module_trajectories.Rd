% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_longitudinal_quick.R
\name{hc_longitudinal_step3_meta_module_trajectories}
\alias{hc_longitudinal_step3_meta_module_trajectories}
\title{Longitudinal step 3: module trajectories by meta-cluster}
\usage{
hc_longitudinal_step3_meta_module_trajectories(
  hc,
  slot_name = "longitudinal_endotypes",
  facet_ncol = 4,
  free_y = TRUE,
  square_panels = TRUE,
  value_mode = c("scaled_mean_vst", "expression"),
  value_range = c(-2, 2)
)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment}.}

\item{slot_name}{Satellite slot containing meta-clustering outputs. Use a
concrete slot name, a shared prefix such as \code{"longitudinal_endotypes"} to
process matching suffixed slots, or \code{"all"} to run over every compatible
step 2 slot in \code{hc@satellite}.}

\item{facet_ncol}{Number of facet columns.}

\item{free_y}{Logical. If \code{TRUE}, use free y-scales per module.}

\item{square_panels}{Logical. If \code{TRUE}, draw square panels.}

\item{value_mode}{One of \code{"scaled_mean_vst"} or \code{"expression"}.}

\item{value_range}{Optional numeric vector of length 2 for the y-axis range
when \code{value_mode = "scaled_mean_vst"}. Default is \code{c(-2, 2)}. Use \code{NULL}
for automatic scaling.}
}
\value{
A list with unchanged \code{hc} and the step 3 plot. When multiple slots
are processed, \code{plots} is returned as a named list keyed by layer/slot,
plus a \code{slot_info} table describing the resolved slots.
}
\description{
Plots module trajectories after meta-clustering using donor and meta-cluster
mean waves.
}
\examples{
hc <- hc_example_data("clustered")
hc <- hc_longitudinal_step1_module_donor(
  hc,
  donor_col = "donor",
  time_col = "timepoint",
  time_levels = c("T1", "T2"),
  k = 2,
  nstart = 1,
  cap_runs = 1,
  impute = FALSE,
  seed = 1
)$hc
hc <- hc_longitudinal_endotype_clustering(hc, k = 2, nstart = 1, seed = 1)
hc <- hc_longitudinal_meta_clustering(
  hc,
  feature_source = "feature_matrix_used",
  k = 2:3,
  method = "kmeans",
  consensus = TRUE,
  seed = 1
)
res <- hc_longitudinal_step3_meta_module_trajectories(hc)
}

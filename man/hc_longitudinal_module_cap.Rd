% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_longitudinal_endotypes.R
\name{hc_longitudinal_module_cap}
\alias{hc_longitudinal_module_cap}
\title{Compute module-level CAP matrix from repeated trajectory clustering}
\usage{
hc_longitudinal_module_cap(
  hc,
  slot_name = "longitudinal_endotypes",
  output_slot = slot_name,
  runs = 50,
  method = c("kmeans", "hclust"),
  nstart = 30,
  seed = 42,
  scale_features = TRUE,
  na_impute = c("median", "zero"),
  jitter_sd = 0
)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment}.}

\item{slot_name}{Satellite slot containing outputs from
\code{hc_longitudinal_endotype_clustering()}.}

\item{output_slot}{Satellite slot to write results to. Defaults to
\code{slot_name} (in-place update).}

\item{runs}{Number of repeated clustering runs per module (for \code{kmeans}).}

\item{method}{One of \code{"kmeans"} or \code{"hclust"} for repeated module
clustering.}

\item{nstart}{\code{kmeans} random starts per run.}

\item{seed}{Random seed.}

\item{scale_features}{Logical. If \code{TRUE}, z-score module trajectories before
repeated clustering.}

\item{na_impute}{One of \code{"median"} or \code{"zero"} for NA imputation in module
trajectory matrices.}

\item{jitter_sd}{Optional Gaussian jitter SD added to features per run
(\code{kmeans} only) to probe local assignment stability.}
}
\value{
Updated \code{HCoCenaExperiment} with CAP outputs in
\code{hc@satellite[[output_slot]]}.
}
\description{
CAP (cluster assignment proportions) quantifies for each donor the
proportion of repeated clustering runs assigned to each module-specific
cluster. This is the generalized, data-agnostic analogue of the CAP concept
used in the legacy longitudinal scripts.
}
\examples{
hc <- hc_example_data("clustered")
long <- hc_longitudinal_step1_module_donor(
  hc,
  donor_col = "donor",
  time_col = "timepoint",
  time_levels = c("T1", "T2"),
  k = 2,
  nstart = 1,
  cap_runs = 1,
  impute = FALSE,
  seed = 1
)
hc <- long$hc
hc <- hc_longitudinal_module_cap(hc, runs = 1)
}

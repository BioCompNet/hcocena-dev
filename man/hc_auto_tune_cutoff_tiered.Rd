% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_auto_tune_cutoff_tiered.R
\name{hc_auto_tune_cutoff_tiered}
\alias{hc_auto_tune_cutoff_tiered}
\title{Tiered and robust cutoff recommendation}
\usage{
hc_auto_tune_cutoff_tiered(
  hc,
  apply = FALSE,
  tier1_sft_min = 0.85,
  tier1_mean_k_min = 10,
  tier2_sft_min = 0.8,
  tier2_mean_k_min = 5,
  fallback_sft_min = 0.7,
  require_negative_slope = TRUE,
  max_no_networks = NULL,
  max_components = NULL,
  min_node_fraction = NULL,
  fallback_respect_base_filters = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment} object.}

\item{apply}{Logical; if \code{TRUE}, apply the resulting cutoff vector with
\code{hc_set_cutoff()}. Missing tiered recommendations fall back to existing
cutoffs from \code{hc@config@layer$cutoff} where possible.}

\item{tier1_sft_min}{Numeric minimum SFT fit (\code{R.squared}) for the \code{strict}
rule.}

\item{tier1_mean_k_min}{Numeric minimum mean connectivity (\code{mean_k}) for the
\code{strict} rule.}

\item{tier2_sft_min}{Numeric minimum SFT fit (\code{R.squared}) for the
\code{relaxed} rule.}

\item{tier2_mean_k_min}{Numeric minimum mean connectivity (\code{mean_k}) for the
\code{relaxed} rule.}

\item{fallback_sft_min}{Numeric lower bound for \code{best_available} selection
using the maximum available SFT fit.}

\item{require_negative_slope}{Logical; require a negative slope (\code{slope < 0})
in Tier 1/Tier 2 (and optionally fallback, see
\code{fallback_respect_base_filters}).}

\item{max_no_networks}{Optional numeric scalar. If set, only cutoffs with
\code{no_of_networks <= max_no_networks} are accepted in Tier 1/Tier 2.}

\item{max_components}{Deprecated alias for \code{max_no_networks} (kept for
backward compatibility).}

\item{min_node_fraction}{Optional numeric in \verb{(0, 1]}. If set, require
\code{no_nodes >= min_node_fraction * max(no_nodes)} in Tier 1/Tier 2.}

\item{fallback_respect_base_filters}{Logical; if \code{TRUE}, \code{best_available}
selection is restricted by base filters (\code{slope}, \code{max_no_networks},
\code{min_node_fraction}).}

\item{verbose}{Logical; print per-layer recommendation messages.}
}
\value{
Updated \code{HCoCenaExperiment} with a report in
\code{hc@satellite$cutoff_tuning}.
}
\description{
Alternative cutoff tuning method that can be compared against the current
simple cutoff recommendation.
}
\details{
For each layer, the function:
\enumerate{
\item Summarizes cutoff statistics.
\item Computes additional diagnostics (\code{slope}, \code{mean_k}).
\item Applies a staged rule set (\code{strict}, \code{relaxed}, \code{best_available}).
}

Results are stored in \code{hc@satellite$cutoff_tuning}
(and mirrored to \code{hc@satellite$auto_tune_cutoff_tiered} for compatibility).
}
\examples{
hc <- hc_example_data("after_part1")
hc <- hc_auto_tune_cutoff_tiered(hc, apply = FALSE, verbose = FALSE)
}

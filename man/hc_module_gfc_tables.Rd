% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_module_gfc_tables.R
\name{hc_module_gfc_tables}
\alias{hc_module_gfc_tables}
\title{Module GFC per group and per sample}
\usage{
hc_module_gfc_tables(
  hc,
  layer = 1,
  group_col = NULL,
  modules = "all",
  annotate = TRUE,
  export_xlsx = TRUE,
  out_dir = NULL,
  file_name = "Module_GFC_group_and_sample.xlsx",
  verbose = TRUE
)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment}.}

\item{layer}{Layer to use, either its index or its name. Defaults to the
first layer.}

\item{group_col}{Annotation column used for grouping. Defaults to the
\code{variable_of_interest} from the global settings.}

\item{modules}{Either \code{"all"} (default) or a vector of module labels.}

\item{annotate}{Logical; if \code{TRUE} (default), the layer annotation is
appended to the long-format sample table so it can be grouped directly.}

\item{export_xlsx}{Logical; if \code{TRUE} (default), all tables are written to
an Excel workbook.}

\item{out_dir}{Target folder for the workbook. If \code{NULL} (default), the save
folder of \code{hc} is used.}

\item{file_name}{File name of the workbook.}

\item{verbose}{Logical; print the self-check and the export path.}
}
\value{
Updated \code{HCoCenaExperiment}. The tables are stored in
\code{hc@satellite$module_gfc_tables} as \code{by_group}, \code{by_group_long},
\code{by_sample}, \code{by_sample_long}, plus \code{reference} and \code{settings}.
}
\description{
Returns the Group-Fold-Change values behind the module heatmap as tables:
once aggregated per group (the numbers shown in the heatmap) and once
resolved per individual sample.
}
\details{
hCoCena itself only stores the group level (\code{module_gfc_means}, modules by
groups). This function computes the per-sample counterpart using hCoCena's
own GFC definition, so both tables live on the same scale:
\enumerate{
\item when \code{data_in_log} is \code{TRUE}, values are anti-logged (base 2) and all
averaging happens on the linear scale,
\item group means (or the raw sample values) are formed per gene,
\item the reference follows the \code{control_keyword} global setting, exactly as
\code{\link[=hc_run_expression_analysis_2]{hc_run_expression_analysis_2()}} does. With \code{"none"} it is the unweighted
mean of the \strong{group} means -- not the mean across all samples, which
differs whenever the groups are unequally sized. With a control keyword
it is the mean of the matching control group, and that group is dropped
from the group table just like in the heatmap,
\item \code{foldchange(x, ref)} is \code{x / ref} when \code{x >= ref} and \code{-ref / x}
otherwise, a signed ratio rather than a difference of logs,
\item values are clamped to \verb{+/- range_GFC},
\item the module value is the mean across the genes of that module.
}

Because step 4 is non-linear and step 5 truncates, \strong{the group table is not
the average of the sample table} (\code{fc(mean(x)) != mean(fc(x))}). Both are
correct but answer different questions: \code{by_group} is the value plotted in
the heatmap, \code{by_sample} shows how uniformly the samples carry it. Use
\code{by_sample} for sample-level statistics and \code{by_group} for figure labels.

As a self-check the function compares its own \code{by_group} against the
\code{grp_means} stored by hCoCena and reports the largest absolute difference,
which is expected to be on the order of \code{1e-3} -- the rounding hCoCena
applies per gene.
}
\examples{
hc <- hc_example_data("clustered")
hc <- hc_module_gfc_tables(hc, export_xlsx = FALSE)
hc_satellite(hc, "module_gfc_tables")$by_group
}
\seealso{
\code{\link[=hc_plot_cluster_heatmap]{hc_plot_cluster_heatmap()}}, which produces the group-level
\code{module_gfc_means} table.
}

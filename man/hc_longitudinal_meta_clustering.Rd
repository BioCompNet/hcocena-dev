% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_longitudinal_endotypes.R
\name{hc_longitudinal_meta_clustering}
\alias{hc_longitudinal_meta_clustering}
\title{Meta-cluster donors based on longitudinal CAP features}
\usage{
hc_longitudinal_meta_clustering(
  hc,
  slot_name = "longitudinal_endotypes",
  output_slot = slot_name,
  feature_source = c("cap_matrix", "module_cluster_matrix", "feature_matrix_used"),
  k = 2:8,
  method = c("kmeans", "hclust"),
  scale_features = TRUE,
  nstart = 50,
  seed = 42,
  na_impute = c("median", "zero"),
  cluster_prefix = "MC",
  consensus = TRUE,
  consensus_runs = 200,
  consensus_sample_fraction = 0.8,
  consensus_feature_fraction = 0.8,
  consensus_linkage = "average",
  compute_umap = TRUE,
  umap_neighbors = 15,
  umap_min_dist = 0.3
)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment}.}

\item{slot_name}{Satellite slot containing longitudinal outputs.}

\item{output_slot}{Satellite slot to write results to (default in-place).}

\item{feature_source}{One of \code{"cap_matrix"}, \code{"module_cluster_matrix"}, or
\code{"feature_matrix_used"}.}

\item{k}{Candidate meta-cluster numbers.}

\item{method}{One of \code{"kmeans"} or \code{"hclust"}.}

\item{scale_features}{Logical. If \code{TRUE}, z-score features before clustering.}

\item{nstart}{Number of random starts for \code{kmeans}.}

\item{seed}{Random seed.}

\item{na_impute}{One of \code{"median"} or \code{"zero"}.}

\item{cluster_prefix}{Prefix for meta-cluster labels.}

\item{consensus}{Logical. If \code{TRUE}, run consensus clustering with repeated
subsampling for each candidate \code{k} and select the best \code{k} via consensus
separation.}

\item{consensus_runs}{Number of consensus resampling runs per candidate \code{k}.}

\item{consensus_sample_fraction}{Fraction of donors sampled per consensus run.}

\item{consensus_feature_fraction}{Fraction of features sampled per consensus
run.}

\item{consensus_linkage}{Linkage method for final hierarchical clustering on
the consensus distance (\code{1 - consensus}).}

\item{compute_umap}{Logical. If \code{TRUE}, compute UMAP coordinates when
\code{uwot} or \code{umap} is available.}

\item{umap_neighbors}{UMAP neighborhood size.}

\item{umap_min_dist}{UMAP minimum distance.}
}
\value{
Updated \code{HCoCenaExperiment}.
}
\description{
Clusters donors on CAP features (or alternative longitudinal feature
matrices), providing CH-based model selection and PCA/UMAP embeddings.
}
\examples{
hc <- hc_example_data("clustered")
hc <- hc_longitudinal_step1_module_donor(
  hc,
  donor_col = "donor",
  time_col = "timepoint",
  time_levels = c("T1", "T2"),
  k = 2,
  nstart = 1,
  cap_runs = 1,
  impute = FALSE,
  seed = 1
)$hc
hc <- hc_longitudinal_endotype_clustering(hc, k = 2, nstart = 1, seed = 1)
hc <- hc_longitudinal_meta_clustering(
  hc,
  feature_source = "feature_matrix_used",
  k = 2:3,
  method = "kmeans",
  seed = 1
)
}

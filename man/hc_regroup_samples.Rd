% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_regroup_samples_s4.R
\name{hc_regroup_samples}
\alias{hc_regroup_samples}
\title{Regroup samples from module-level fold-change profiles}
\usage{
hc_regroup_samples(
  hc,
  layer = "all",
  modules = "all",
  k = 2,
  reference = c("mean", "control"),
  method = "complete",
  distance = "euclidean",
  group_col = "hc_regroup",
  apply = FALSE,
  overwrite = FALSE,
  show_values = FALSE,
  silent = !base::interactive()
)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment} with clustered modules.}

\item{layer}{Layer selection. Use \code{"all"} (default), a layer index, layer id,
configured layer name, or a vector of these values.}

\item{modules}{Modules used as clustering features. Use \code{"all"} (default),
current module labels such as \code{"M2"} or \code{"M2.1"}, module colors, or
numeric module indices.}

\item{k}{Number of sample groups after cutting the dendrogram. Supply one
integer for all selected layers or one integer per selected layer. Named
vectors may use layer ids or configured layer names.}

\item{reference}{Reference used for sample-level fold changes. \code{"mean"}
compares each sample with the gene mean across all samples in the layer.
\code{"control"} compares with the mean of samples matching the configured
control label in the current variable-of-interest column.}

\item{method}{Hierarchical clustering method passed to \code{\link[stats:hclust]{stats::hclust()}}.}

\item{distance}{Distance used between samples. Supports methods accepted by
\code{\link[stats:dist]{stats::dist()}} and \code{"correlation"} for one minus Pearson correlation.}

\item{group_col}{Name of the new annotation column created when
\code{apply = TRUE}.}

\item{apply}{Logical. If \code{FALSE} (default), return a preview without changing
\code{hc}. If \code{TRUE}, add the inferred groups to \code{group_col} in each selected
layer and store the diagnostics in \code{hc@satellite$sample_regrouping}.}

\item{overwrite}{Logical. Allow an existing \code{group_col} to be replaced when
\code{apply = TRUE}. Defaults to \code{FALSE}.}

\item{show_values}{Logical. Display rounded fold-change values in heatmap
cells.}

\item{silent}{Logical. If \code{FALSE}, draw each generated heatmap. The default
is \code{!interactive()} so plots are shown in interactive R sessions.}
}
\value{
A list with \code{hc}, per-layer \code{results}, \code{plots}, \code{parameters}, and
\code{applied}. Each layer result contains \code{sample_gfc}, \code{module_table},
\code{distance}, \code{tree}, \code{clusters}, and \code{plot}.
}
\description{
Builds a module-by-sample fold-change matrix for one or more layers, clusters
samples hierarchically, and cuts each dendrogram into \code{k} groups. By default,
this is a non-destructive preview: the input object, its variable of interest,
module assignments, and annotation files are not changed.
}
\details{
Fold changes are calculated for every gene in every sample relative to either
that gene's mean across the layer or its mean in the configured control
samples. Gene-level values are then averaged within the current modules. This
follows the signed-ratio convention used by hCoCena: two-fold down is \code{-2}, no
change is \code{1}, and two-fold up is \code{2}.
}
\examples{
hc <- hc_example_data("clustered")
preview <- hc_regroup_samples(hc, layer = 1, k = 2, silent = TRUE)
preview$results$set1$clusters
preview$results$set1$sample_gfc

applied <- hc_regroup_samples(
  hc,
  layer = "all",
  k = 2,
  group_col = "module_profile_group",
  apply = TRUE,
  silent = TRUE
)
hc <- applied$hc

}

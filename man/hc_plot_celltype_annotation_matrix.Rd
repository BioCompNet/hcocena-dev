% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_plot_celltype_annotation_matrix.R
\name{hc_plot_celltype_annotation_matrix}
\alias{hc_plot_celltype_annotation_matrix}
\title{Plot module x cell-type matrix from module cell-type annotation}
\usage{
hc_plot_celltype_annotation_matrix(
  hc,
  database = NULL,
  value = c("pct", "count"),
  top_celltypes = 15,
  include_other = TRUE,
  other_label = "Other",
  cluster_order = NULL,
  low_color = "#f7fbff",
  high_color = "#08306b",
  return_data = TRUE
)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment}.}

\item{database}{Optional single database name. If \code{NULL}, uses combined
\code{selected_celltypes} across databases.}

\item{value}{One of \code{"pct"} or \code{"count"}.}

\item{top_celltypes}{Maximum number of cell types to show. Remaining cell
types can be collapsed into \code{other_label} when \code{include_other = TRUE}.}

\item{include_other}{Logical. Collapse non-top cell types into one column.}

\item{other_label}{Label for the collapsed column.}

\item{cluster_order}{Optional explicit module order (character vector).}

\item{low_color}{Low-end fill color.}

\item{high_color}{High-end fill color.}

\item{return_data}{Logical. If \code{TRUE}, return a list with \code{plot}, \code{matrix},
and \code{long_data}; otherwise return only the plot.}
}
\value{
A \code{ggplot} object or a list containing plot + data.
}
\description{
Uses \code{hc@satellite$celltype_annotation} and draws a heatmap-like matrix
(modules x cell types) for easier interpretation than stacked barplots.
}
\examples{
hc <- hc_example_data("clustered")
gmt <- system.file("extdata", "toy_celltype_markers.gmt", package = "hcocena")
hc <- hc_celltype_annotation(
  hc,
  databases = character(0),
  custom_gmt_files = c(CellTypes = gmt),
  export_excel = FALSE
)
p <- hc_plot_celltype_annotation_matrix(hc)
}

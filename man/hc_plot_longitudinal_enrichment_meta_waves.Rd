% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_longitudinal_endotypes.R
\name{hc_plot_longitudinal_enrichment_meta_waves}
\alias{hc_plot_longitudinal_enrichment_meta_waves}
\title{Plot longitudinal enrichment waves by meta-cluster}
\usage{
hc_plot_longitudinal_enrichment_meta_waves(
  hc,
  slot_name = "longitudinal_endotypes",
  databases = "Kegg",
  top = 3,
  custom_terms = NULL,
  term_match = c("exact", "contains", "regex"),
  enrichment_table = c("all", "selected", "significant"),
  score_method = c("ssgsea", "zscore", "rank_mean"),
  score_scale = c("none", "z"),
  layer = NULL,
  donor_col = NULL,
  time_col = NULL,
  time_levels = NULL,
  min_term_genes = 5,
  impute_missing = c("auto", "linear", "locf", "none"),
  qvalue_max = NULL,
  show_donor_lines = TRUE,
  donor_alpha = 0.04,
  donor_linewidth = 0.18,
  mean_linewidth = 2,
  mean_point_size = 1.8,
  show_ci = FALSE,
  ci_level = 0.95,
  ci_alpha = 0.12,
  free_y = TRUE,
  save_pdf = TRUE,
  file_prefix = "Longitudinal_Enrichment_MetaWaves",
  save_width = 12,
  save_height = 12,
  export_excel = TRUE
)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment}.}

\item{slot_name}{Satellite slot with \code{meta_cluster} results. Default:
\code{"longitudinal_endotypes"}. Use a concrete slot name, a shared prefix
such as \code{"longitudinal_endotypes"} to process matching suffixed slots, or
\code{"all"} to run over every compatible step 2 slot in \code{hc@satellite}.}

\item{databases}{Character vector of databases to visualize (e.g. \code{"Kegg"}).}

\item{top}{Integer number of top terms per module.}

\item{custom_terms}{Optional custom term filter (character vector or named
module list). If set, matched terms are used instead of automatic top-term
selection.}

\item{term_match}{Matching mode for \code{custom_terms}: \code{"exact"},
\code{"contains"}, or \code{"regex"}.}

\item{enrichment_table}{Source enrichment table: \code{"all"} (default),
\code{"selected"}, or \code{"significant"}.}

\item{score_method}{\code{"ssgsea"} (default), \code{"zscore"}, or \code{"rank_mean"}.}

\item{score_scale}{Plot-only scaling: \code{"none"} (default) or \code{"z"} to
z-standardize scores within each module-term before plotting.}

\item{layer}{Optional layer selector forwarded to
\code{\link[=hc_plot_longitudinal_enrichment_waves]{hc_plot_longitudinal_enrichment_waves()}}.}

\item{donor_col, time_col, time_levels}{Optional overrides forwarded to
\code{\link[=hc_plot_longitudinal_enrichment_waves]{hc_plot_longitudinal_enrichment_waves()}}.}

\item{min_term_genes}{Minimum matched genes per term.}

\item{impute_missing}{Missing-value handling.}

\item{qvalue_max}{Optional q-value cutoff before top-term selection.}

\item{show_donor_lines}{Logical; include faint donor trajectories.}

\item{donor_alpha, donor_linewidth}{Styling for donor lines.}

\item{mean_linewidth, mean_point_size}{Styling for meta-cluster means.}

\item{show_ci}{Logical; if \code{TRUE}, draw subtle confidence ribbons around
meta-cluster mean trajectories.}

\item{ci_level}{Confidence level in \verb{(0,1)} for ribbons (default \code{0.95}).}

\item{ci_alpha}{Ribbon alpha for confidence bands.}

\item{free_y}{Logical; use free y scales per module row.}

\item{save_pdf}{Logical; save one PDF per selected database.}

\item{file_prefix}{Prefix for saved files.}

\item{save_width, save_height}{PDF size.}

\item{export_excel}{Logical; export tables per database.}
}
\value{
A list with \code{plots}, \code{top_terms}, \code{donor_trajectories},
\code{mean_trajectories}, and \code{score_method_used}. When multiple slots are
processed, these top-level components are flattened with slot-prefixed
names for convenient iteration, and nested per-slot outputs are also
returned in \code{results_by_slot}, together with \code{slot_info}.
}
\description{
Creates one plot per enrichment database. For each module, the top \code{x} terms
are placed into dedicated side-by-side panels (\verb{Top 1}, \verb{Top 2}, ...), and
meta-cluster trajectories are drawn as longitudinal waves.
}
\examples{
hc <- hc_example_data("clustered")
gmt <- system.file("extdata", "toy_celltype_markers.gmt", package = "hcocena")
hc <- hc_functional_enrichment(
  hc,
  gene_sets = character(0),
  custom_gmt_files = c(CellTypes = gmt),
  universe = "network"
)
hc <- hc_longitudinal_step1_module_donor(
  hc,
  donor_col = "donor",
  time_col = "timepoint",
  time_levels = c("T1", "T2"),
  k = 2,
  nstart = 1,
  cap_runs = 1,
  impute = FALSE,
  seed = 1
)$hc
hc <- hc_longitudinal_endotype_clustering(hc, k = 2, nstart = 1, seed = 1)
hc <- hc_longitudinal_meta_clustering(
  hc,
  feature_source = "feature_matrix_used",
  k = 2:3,
  method = "kmeans",
  consensus = TRUE,
  seed = 1
)
waves <- hc_plot_longitudinal_enrichment_meta_waves(
  hc,
  databases = "Celltypes",
  top = 1,
  min_term_genes = 3,
  score_method = "rank_mean",
  save_pdf = FALSE,
  export_excel = FALSE
)
}

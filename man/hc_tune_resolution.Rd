% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hc_tune_resolution.R
\name{hc_tune_resolution}
\alias{hc_tune_resolution}
\title{Tune clustering algorithm and resolution}
\usage{
hc_tune_resolution(
  hc,
  apply = FALSE,
  cluster_algorithms = c("cluster_leiden", "cluster_louvain"),
  resolution_grid = base::seq(0.05, 0.5, by = 0.05),
  no_of_iterations = 2,
  partition_type = "RBConfigurationVertexPartition",
  max_cluster_count_per_gene = 1,
  module_count_bounds = c(3, 60),
  prefer_resolution = 0.1,
  verbose = TRUE
)
}
\arguments{
\item{hc}{A \code{HCoCenaExperiment} object.}

\item{apply}{Logical; if \code{TRUE}, apply the best clustering recommendation.}

\item{cluster_algorithms}{Character vector of candidate algorithms.}

\item{resolution_grid}{Numeric vector of Leiden resolutions.}

\item{no_of_iterations}{Passed to clustering.}

\item{partition_type}{Passed to clustering.
For Leiden, resolution sweeps are only informative with
\code{"CPMVertexPartition"} or \code{"RBConfigurationVertexPartition"}.}

\item{max_cluster_count_per_gene}{Passed to clustering.}

\item{module_count_bounds}{Optional length-2 numeric vector
(\code{min_modules}, \code{max_modules}) used as a soft constraint.}

\item{prefer_resolution}{Tie-break preference for resolution.}

\item{verbose}{Logical; print progress messages.}
}
\value{
Updated \code{HCoCenaExperiment}.
}
\description{
Convenience wrapper for clustering-focused auto-tuning.
Internally this calls \code{hc_auto_tune(..., tune_cutoff = FALSE, tune_clustering = TRUE)}.
The clustering report is additionally mirrored to \code{hc@satellite$resolution_tuning}.
}
\examples{
hc <- hc_example_data("clustered")
hc <- hc_tune_resolution(hc, apply = FALSE)
}
